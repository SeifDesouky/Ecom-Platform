namespace EcomPlatform.Core.Enums
{
    public enum MarketplacePlatform
    {
        // ── المنصات العربية ───────────────────────────────────────────────────
        Salla = 1,              // سلة
        Zid = 2,                // زد
        Matjar = 3,             // متجر
        ExpandCart = 4,         // إكسباند كارت
        Tagger = 5,             // تاجر
        YouCan = 6,             // يوكان
        Toggar = 7,             // تجار
        ExpandCartEgypt = 8,    // إكسباند كارت مصر
        Shopini = 9,            // شوبيني
        PaycornStore = 10,      // بايكورن ستور
        Makhazin = 11,          // مخازن الإلكترونية
        Noon = 12,              // نون
        NoonExpress = 13,       // نون اكسبرس
        Jarir = 14,             // جارير
        TikTokShop = 15,        // تيك توك شوب
        InstagramShop = 16,     // إنستجرام شوب
        WhatsAppCatalog = 17,   // واتساب بزنس كتالوج
        GoogleShopping = 18,    // قوقل للتسوق

        // ── الأسواق الكبرى ────────────────────────────────────────────────────
        Amazon = 100,
        Ebay = 101,
        WalmartMarketplace = 102,
        Etsy = 103,

        // ── منصات بناء المتاجر ────────────────────────────────────────────────
        Shopify = 200,
        WooCommerce = 201,
        Magento = 202,
        BigCommerce = 203,
        SquarespaceCommerce = 204,

        // ── منصات آسيا ───────────────────────────────────────────────────────
        Lazada = 300,
        Shopee = 301,
        AliExpress = 302,

        // ── منصات متخصصة ─────────────────────────────────────────────────────
        FacebookShop = 400,
        Shein = 401,
    }
}